# Database Configuration (for Docker PostgreSQL)
DB_HOST=db
DB_USER=myrunnerslist
DB_PASSWORD=myrunnerslist
DB_NAME=myrunnerslist

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_key_here

# Internal API Key for scraper authentication
INTERNAL_API_KEY=your_internal_api_key_here

# Optional: Upstash / Redis — race list cache + future rate limiting (empty = disabled)
# REDIS_URL=rediss://default:token@host:6379